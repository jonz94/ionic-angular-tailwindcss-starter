import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-tailwind-starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
